# APP_FACTORY.yaml — 프로젝트 전체 설정의 기준 파일 (SSOT)
# 스키마: app-factory-config.schema.json v1
# 이 파일 수정 후에는 factory auto가 스냅샷과의 차이를 감지해 반영한다.

{{app_factory_yaml}}
# ↑ 인터뷰 결과 + defaults.yaml 병합 결과를 스키마 검증 후 직렬화해 삽입한다.
